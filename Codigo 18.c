#include <stdio.h>

float const PI=3.14
int main(){

    float area, radio;
    prinf("ingrese el valor del radio de la circunferencia:\n");
    scanf("%f", &radio);
    area = pi * radio * radio;
    printf("el area de la circunferencia es de %f", area);

    return 0;
}